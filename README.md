# PdfMiningInDocker
The structure of the code is as below:

PdfMiningInDocker \
`--my_celery`\
`--__init__.py`\
`--celery.py` \
`--tasks.py` \
`--docker-compose.yml` \
`--dockerfile` \
`--requirements.txt` \
